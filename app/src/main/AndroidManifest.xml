<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="goldzweigapps.com.seatback">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.SeatBackApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
        </activity>

        <service android:name=".services.TimeService" />

        <activity android:name=".activities.InformationActivity" />
        <activity android:name=".activities.WorkoutActivity" />
        <!-- crashlytics api key -->
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="e009a9b8d7f77fc4978beab18a724ba254280741" />

        <activity android:name=".activities.SplashScreeen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>